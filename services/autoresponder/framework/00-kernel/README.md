# 00 - Kernel

O Kernel é o núcleo executor do framework. Ele é responsável por orquestrar o ciclo de vida de cada mensagem recebida, coordenando o pré-processamento, carregamento de contexto, roteamento de skills, execução de actions, validação e envio.

Este módulo não deve conter regras de negócio/comerciais.
