# v1.2.254 — Corrige schema de pedidos

- Data: 17/08/2026
- Status: pronto para publicação
- Branch: main
- API: `mdv-api` (restart com migration automática)
- Tag: `v1.2.254-corrige-schema-pedidos`

## Correção

- adiciona as colunas `orders.shipping_origin_cep` e `orders.shipping_origin_label` no início da API, apenas quando ainda não existirem;
- restaura a criação de pedidos do checkout que salva a origem do frete;
- preserva os pedidos existentes e não altera valores, estoque ou pagamento.

## Proteção regressiva

O teste de pedidos exige que os dois entrypoints da API registrem a migration antes do checkout persistir os campos.

## Validações

- `node tmp-tests/orders-service-vps-static.test.mjs`
- `node tmp-tests/cart-create-order-customer-proxy-static.test.mjs`
- `node --check vps_server.js`
- `node --check vps_server.cjs`
- status público da API e schema `orders` após o deploy.
