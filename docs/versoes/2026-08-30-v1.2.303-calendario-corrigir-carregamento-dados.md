# v1.2.303 — Correção no Carregamento de Dados do Calendário de Marketing

- data: 2026-08-30
- status: ready
- branch: main
- release_vps: pendente
- tag: v1.2.303-calendario-corrigir-carregamento-dados

## Escopo

- Correção no parsing do retorno de `marketingApprovalService.list('all')` em `MarketingCalendarPanel.tsx` para evitar exceção de iteração em objetos de resposta com contadores.
- Adição de `safeSchedules` e `safeApprovals` com verificação de tipo de lista `Array.isArray` defensiva.
- Eliminação do toast de erro indevido durante carregamento com listas parciais.

## Validações

- `npm run build`;
- `git diff --check`.
