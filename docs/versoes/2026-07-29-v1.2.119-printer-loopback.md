# v1.2.119 — Acesso IPv4 ao serviço de impressão

```text
date: 2026-07-29
status: ready
branch: main
release_vps: /var/www/mdv-site/releases/20260729-151437-v12119-printer-loopback
tag: v1.2.119-printer-loopback
```

## Alterações

- o painel usa explicitamente `127.0.0.1:8081` para falar com o serviço local;
- evita a resolução de `localhost` para IPv6 (`::1`) quando o serviço está protegido no loopback IPv4;
- mantém o acesso restrito ao computador das impressoras.

## Validações

```powershell
node tmp-tests\shopee-dual-thermal-print-flow-static.test.mjs
npm.cmd run build
curl.exe -i http://127.0.0.1:8081/printers
```
