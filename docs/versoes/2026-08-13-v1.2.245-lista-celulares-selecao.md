# v1.2.245-lista-celulares-selecao

Data: 2026-08-13

## Alterações

- Organiza a lista de smartphones por marca e mantém as variações do mesmo modelo adjacentes.
- Preserva uma numeração própria para cada combinação de modelo, memória e preço.
- Remove os links da lista inicial para tornar a leitura mais compacta.
- Ao cliente selecionar um número, envia o resumo da variação, as cores agrupadas e o link correspondente.
- Assume uma unidade por padrão e pergunta diretamente se o cliente prefere retirada ou entrega.
- Permite alterar a quantidade quando o cliente informar explicitamente que deseja mais unidades.
- Evita repetir o resumo e o link depois que o cliente escolhe a cor.

## Fonte de verdade

- Workflow ativo n8n `SkrkB4vyKVDnQ68t`.
- Nós alterados: `Vendas - Contexto Produtos` e `Vendas - Verificar Pos Lista`.
- O catálogo e o estoque continuam sendo consultados pela API oficial do Mercado do Vale.

## Validação

- Teste estático do agrupamento, numeração, links, cor e quantidade.
- Compilação sintática do patch do workflow.
- Dry-run contra o workflow ativo antes da aplicação.
- Sincronização entre `workflow_entity` e `workflow_history` após a publicação.

## Release VPS

`/var/www/mdv-site/releases/20260813-123652-v1245-lista-celulares-selecao`
