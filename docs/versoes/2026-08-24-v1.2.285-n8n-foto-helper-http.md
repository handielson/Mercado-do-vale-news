# v1.2.285 — helper HTTP para foto no n8n

- Data: 2026-08-24
- Branch: `main`
- Status: publicado
- Release VPS do site: `/var/www/mdv-site/releases/20260824-232515-v1-2-285-n8n-foto-helper-http`
- Tag: `v1.2.285-n8n-foto-helper-http`

## Correção

- A atualização de imagens de listas antigas passa a usar `helpers.httpRequest`, fornecido pelo task runner do n8n.
- O fluxo deixa de usar `fetch` e `AbortSignal` dentro do Code node, pois esses globais não são expostos no sandbox do runner 2.35.7.
- O pedido de foto continua preservando o produto, a cor e a etapa comercial em andamento.

## Evidência

- A execução 8070 classificou `pedir_foto` e entrou no tratamento anterior à entrega, mas retornou o texto de imagem ausente.
- O estado salvo continha o SKU correto e `images=[]`.
- A API devolveu o mesmo ID do produto e quatro imagens resolvidas.
- O código do runner confirma `helpers.httpRequest` entre os métodos RPC expostos e não inclui `fetch` ou `AbortSignal` nas variáveis nativas do sandbox.

## Validações

- Teste executa o ramo completo com um mock de `helpers.httpRequest` e confirma a geração da mensagem de imagem.
- Teste impede o retorno de `fetch` e `AbortSignal` na hidratação de fotos.
- Produto divergente continua impedido de fornecer uma foto incorreta.
- Backup dedicado é criado antes da atualização do workflow.
- Nenhuma mensagem é enviada automaticamente ao contato durante a publicação.
