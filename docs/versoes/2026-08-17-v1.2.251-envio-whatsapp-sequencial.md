# v1.2.251 — envio WhatsApp sequencial

Data: 17/08/2026  
Branch: main  
Status: ready  
Release VPS: pendente

## Alteração

- Restaura o `Loop - Enviar mensagens em ordem` para respostas com várias mensagens.
- Configura o `Split In Batches` v3 com lote de um item e usa somente a saída `loop` para o caminho de envio.
- Retorna ao loop somente depois de `Handoff - Registrar bot enviado`, impedindo que a Evolution receba todos os blocos do catálogo em paralelo.

## Proteção e validação

- `tmp-tests/n8n-fix-catalog-order-and-model-match-static.test.mjs` confirma que a saída `done` fica vazia, a saída `loop` chega ao verificador e o retorno pós-envio volta ao loop.
- Workflow ativo e histórico ativo atualizados juntos; n8n e runner retornaram a `1/1` e `/healthz` respondeu `ok`.

## Arquivos da entrega

- `tmp-tests/n8n-fix-catalog-order-and-model-match.cjs`
- `tmp-tests/n8n-fix-catalog-order-and-model-match-static.test.mjs`
- `public/VERSION.json`
- `VERSAO_ATUAL.md`
