# v1.2.156-meta-env-seguro

- Data: 2026-08-04
- Branch: `main`
- Tag: `v1.2.156-meta-env-seguro`
- Release VPS: `/var/www/mdv-site/releases/20260804-153115-meta-env-seguro`

## Alterações

- configura `META_GRAPH_API_VERSION=v25.0` quando ainda não houver versão definida;
- configura o callback oficial `https://api.xiaomipetrolina.com.br/integrations/meta/oauth/callback`;
- gera uma chave de criptografia de 256 bits para tokens somente quando ainda não existir;
- preserva valores remotos existentes em novas publicações;
- não grava nem substitui `META_APP_ID` ou `META_APP_SECRET` quando estiverem ausentes localmente.

## Segurança

Nenhum segredo é incluído no Git, nos registros de versão ou na saída pública. A integração permanece inativa até a configuração legítima de `META_APP_ID` e `META_APP_SECRET`.

## Validações

- `node tmp-tests/meta-vps-env-static.test.mjs`
- `node --check deploy-vps-server-only.cjs`
- `node --check server.js`
- `node --check vps_server.js`
- `node --check vps_server.cjs`
- `npm.cmd run build`
- API pública com HTTP 200 e `mysql.ok=true`
